import 'package:flutter/material.dart';

class RegisterTaskScreen extends StatefulWidget {
  @override
  _RegisterTaskScreen createState() => _RegisterTaskScreen();
}

class _RegisterTaskScreen extends State<RegisterTaskScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // backgroundColor: Color(0xFF020228),
      backgroundColor: Colors.yellow,
      body: SafeArea(
        child: Column(

        ),
      ),
    );
  }
}